# Qwen3-VL third-party notices

This notice catalog accompanies RawCull's converted `qwen3_vl_2b` asset,
including its language, embedding, vision, and tokenizer resources.

## Components and licences

- Qwen3-VL-2B-Instruct and its tokenizer resources are distributed under the
  Apache License 2.0. The complete licence is in
  `Qwen3-VL-Apache-2.0.txt`.
- The conversion recipe was adapted from Apple `coreai-models`. Its complete
  BSD 3-Clause notice is in `Apple-coreai-models-BSD-3-Clause.txt`.

The upstream model reference, conversion metadata, and known checksums are in
`PROVENANCE.json`. Preserve this entire directory with every redistributed
copy of the asset pack.

## Modifications and release status

The upstream Qwen checkpoint was converted into three Core AI runtime assets
and packaged with a Core AI-compatible tokenizer. These converted files are
modified object forms of the upstream model.

RawCull publishes this pack in the v3 model release. The release catalog
records its download size and version, while the release host records the
archive checksum.

The model can produce inaccurate or biased results and is provided without
warranty under the accompanying licences.
